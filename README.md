This is the frontend application of the Employee Management System, developed using ReactJS. It communicates with the Spring Boot backend via REST APIs to perform CRUD operations on employee data.

The UI allows users to add, view, filter, and delete employees with a clean and responsive interface.
